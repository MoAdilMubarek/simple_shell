#include "shell.c"

/**
 * execute - execute an user command
 * @str: pointer
 * Return: no thing
 */

int execute(char *str)
{
	char *argv[128], *token;
	int count, execRet, status;
	pid_t child-pid;
}
